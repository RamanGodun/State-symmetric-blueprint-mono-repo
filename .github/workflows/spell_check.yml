name: spell-check
on:
  pull_request:
    paths:
      - "**/*.md"
      - ".github/**"
jobs:
  spell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Run cspell
        uses: streetsidesoftware/cspell-action@v6
        with:
          files: "**/*.md"
          # без config; можна додати ignorePaths, якщо треба
