name: shared_utils
description: >
  Shared utility functions and helpers for all apps in the monorepo.
  Contains common utilities, extensions, helpers, and pure functions.

version: 0.1.0+1
publish_to: none

# =============================
# ENVIRONMENT
# =============================
environment:
  sdk: ^3.10.0

# =============================
# DEPENDENCIES
# =============================
dependencies:
  flutter:
    sdk: flutter
  shared_core_modules:
    path: ../../packages/shared_core_modules
  shared_layers:
    path: ../../packages/shared_layers
  # Core dependencies (if needed)
  # fpdart: ^1.1.0  # Functional programming utilities
  # intl: ^0.18.0   # Internationalization utilities

# =============================
# DEV DEPENDENCIES
# =============================
dev_dependencies:
  build_runner: ^2.5.4
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  test_api: ^0.7.7
  very_good_analysis: ^10.0.0
