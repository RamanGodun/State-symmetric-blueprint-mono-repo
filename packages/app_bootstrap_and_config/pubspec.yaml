name: app_bootstrap_and_config
description: Common codebase for apps bootstra.

version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.8.0
  flutter: ^3.32.0

dependencies:
  device_info_plus: ^11.5.0
  # firebase_core: ^4.0.0
  flutter:
    sdk: flutter
  # flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^9.0.0
