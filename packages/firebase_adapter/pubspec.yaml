name: firebase_adapter
description: >
  Single entry-point adapter around Firebase (init, types, gateways, utils).
  Keeps apps/features backend-agnostic and prevents direct Firebase deps
  leakage.

version: 0.1.0+1
publish_to: none

# =============================
# ENVIRONMENT
# =============================
environment:
  flutter: ">=3.22.0"
  sdk: ^3.8.0

# =============================
# DEPENDENCIES
# =============================
dependencies:
  app_bootstrap:
    path: ../app_bootstrap
  cloud_firestore: ^6.0.0
  core:
    path: ../../packages/core
  firebase_auth: ^6.0.1
  firebase_core: ^4.0.0
  firebase_crashlytics: ^5.0.0
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.2.1
  rxdart: ^0.28.0

# =============================
# DEV DEPENDENCIES
# =============================
dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^9.0.0

# =============================
# FLUTTER CONFIG
# =============================
flutter:
  generate: true
